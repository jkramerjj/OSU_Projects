Implement smithy() method for smithy case.
	Bug: Draw two cards instead of three. (count i start from 1 instead of 0)

Implement adventurer() method for adventurer case.
	Bug: shuffle if the handCount is empty, which should be when the deck is empty.

Implement councilRoomCard() method for council_room case
	Bug: Each player draws a card instead of each other other player draws a card. (Removed the if condition of i!=currentPlayer) 

Implement villageCard() method for village case
	Bug: Number of actions+1 instead of +2

Implement greatHallCase() method for great_hall case



